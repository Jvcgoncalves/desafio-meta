import type { FastifyInstance } from "fastify";

export async function ordersRoutes(_app: FastifyInstance): Promise<void> {
  // Checkout and status routes are implemented with the order capabilities.
}
